import { createStyles } from "@mantine/core";

const useMotherBoardStepStyles = createStyles((theme) => ({
    wrapper : {
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center"
    }
}))

export default useMotherBoardStepStyles