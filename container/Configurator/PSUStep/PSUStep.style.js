import { createStyles } from "@mantine/core";

const usePSUStepStyles = createStyles((theme) => ({
    wrapper : {
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
    }
}))

export default usePSUStepStyles