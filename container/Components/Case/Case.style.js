import { createStyles } from "@mantine/core";

const useCaseStyles = createStyles((theme) => ({
    wrapper: {
        display: "flex",
        padding: "5rem",
        flexDirection: "column",
        alignItems: "center"
    },

    cardSection: {
        paddingTop: "4rem"
    }
}))

export default useCaseStyles;