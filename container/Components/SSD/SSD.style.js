import { createStyles } from "@mantine/core";

const useSSDStyles = createStyles((theme) => ({
    wrapper: {
        padding: "5rem",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
    },

    cardSection: {
        paddingTop: "4rem"
    }
}))

export default useSSDStyles