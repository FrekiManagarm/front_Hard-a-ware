import { createStyles } from "@mantine/core";

const useUsersStats = createStyles((theme) => ({
    wrapper: {
        padding: "1rem 2rem",
        margin: "0 1rem 1rem 1rem",
        background: "rgba(222, 222, 222, 0.7)",
        boxShadow: "0px 0px 10px rgba(0, 0, 0, 0.35)",
    }
}))

export default useUsersStats;